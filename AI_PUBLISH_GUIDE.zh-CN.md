# 用 AI 工具一句话生成分享网址

前面的教程需要你自己上传文件、点设置、等构建。如果你不想碰这些，可以直接让 AI 工具帮你生成网址。

本文覆盖三条路线：**WorkBuddy**、**豆包桌面版**、**百度秒哒 / DuMate**。看完选一条跟着做，全程不需要敲命令。

> 界面名称会随版本更新变化，找不到按钮时，认准 **分享 / 发布 / 生成链接** 这几个词，或者直接在对话框里问 AI：「怎么把这个页面发布成一个网址」。

## 一、先选路线

| | 适合谁 | 要花多久 | 是否免费 | 特点 |
| --- | --- | --- | --- | --- |
| **WorkBuddy** | 已经有本项目文件夹 | 约 1 分钟 | 是 | 直接把本地文件夹发布成网址，改动后重新发布即可 |
| **豆包桌面版** | 想从零让 AI 生成一个页面 | 约 10 分钟 | 是 | 对话式生成，生成完点分享或发布就能拿到链接 |
| **百度秒哒** | 想从零生成，且习惯百度系产品 | 约 10 分钟 | 有免费额度 | 生成后点发布拿公开链接，每日额度以官网规则为准 |
| **DuMate（搭子）** | 需要 AI 操作本地文件、批量处理 | 约 15 分钟 | 有免费积分 | 强在本地自动化，网页交付通常还要再走一次发布 |

**最简单的判断**：文件夹已经在手上 → 用 WorkBuddy；什么都没有、只想描述需求 → 用豆包或秒哒。

---

## 二、路线 A：WorkBuddy（推荐，已有文件夹时最快）

WorkBuddy 可以直接把电脑上做好的文件夹发布成一个网址，无需额外注册 GitHub、无需购买域名；仍需登录 WorkBuddy，并以其当前发布规则为准。

### 步骤

1. 打开 WorkBuddy，把本项目的文件夹作为工作目录打开。
2. 先完成内容定制：把新郎新娘姓名、日期、地点、流程等信息告诉它，让它改 `index.html` 和 `app.js`（[定制教程](./GUIDE.zh-CN.md)里有完整清单）。
3. 确认本地预览没问题后，在对话框里**明确说一句发布的话**，例如：

   > 「帮我把这个项目发布上线，生成一个可以分享的网址」

4. 稍等片刻，它会返回一个 HTTPS 链接，复制即可发给亲友。

### 几个要注意的点

- **必须明确说"发布/上线/生成链接"**。只说"改一下标题"不会被发布，AI 会先改本地文件，再问你要不要更新线上版本 —— 这是保护机制，避免你分享出去的页面被人正在看时被覆盖。
- **改内容后想更新网址**，同样要再说一次「同步更新到线上分享链接」。它会覆盖线上现有内容，链接保持不变。
- **管理入口**：在 WorkBuddy 的 **设置 → 数据管理 → 我发布的应用** 里可以查看和下线已发布的网址。
- 如果返回链接后打不开，等十几秒再刷新一次。

---

## 三、路线 B：豆包桌面版

豆包桌面版可以对话式生成网页，并部署到它自己的服务器上，得到一个固定链接。

### 步骤

1. **切换到工作任务模式**。打开豆包桌面版，在对话框的模式下拉里选择带「工作任务」字样的模式（不同版本可能叫「工作任务 · Turbo」「工作任务 · 本地电脑」等）。这个模式生成网页的效果明显好于普通聊天模式。
2. **调出应用生成能力**。在输入框输入 `/` 调出技能列表，选择「**应用生成**」或带「网页」字样的技能。
3. **粘贴下面的提示词**，把方括号里的内容换成你的真实信息，发送。

   ```text
   请帮我做一份手机端互动婚礼请柬页面，要求如下：

   新郎姓名：［姓名］
   新娘姓名：［姓名］
   英文代号（可写）：［例如 B&G / 两人名字组合］
   婚礼时间：［日期 时间］
   婚礼地点：［地点名称］，地图链接：［高德或百度地图链接］
   交通：［怎么写去，例如地铁几号线哪个口］
   当天流程：
   - ［时间］［事项］
   - ［时间］［事项］
   是否需要宾客登记：［需要 / 不需要］
   想对宾客说的话：［一段真诚的话，没有就请你帮我写］
   背景音乐：［保留模板中的官方音乐试听 / 不需要 / 我提供有权使用的文件］

   页面要求：
   1. 深海探索主题，整体像一场游戏任务：下潜 → 领取婚礼委托 → 声呐探索 → 捕捉祝福鱼 → 查看婚礼日程与地图 → 宾客登记。
   2. 只在手机上看，必须适配 375px、390px、430px 三种宽度，不能出现横向滚动、文字溢出或按钮被遮挡。
   3. 宾客登记只保存在访客自己的手机里，不要发送到任何服务器，也不要接第三方表单。
   4. 不要使用任何游戏角色、动漫形象或受版权保护的素材，全部用 CSS、Emoji 和纯色块绘制。
   5. 不要出现未替换的示例文字和占位符。
   6. 完成后请把它发布成一个网址给我，并告诉我链接地址。
   ```

4. **预览并修改**。生成后点预览查看。有问题不用改代码：点「编辑」，用鼠标框选出问题的区域，直接说「这里的图片没显示，帮我修一下」。
5. **发布拿链接**。两种方式任选：
   - 点右上角「**分享**」，生成一条链接，对方手机直接打开就能看；
   - 在对话框输入 `/` 调出「**网页发布**」类技能，让它部署成固定链接（可用额度与规则以工具当前说明为准）。

### 注意

- 发布后的链接域名由豆包提供（形如 `xxx.aiforce.cloud`），不是你自己的域名，这不影响分享。
- 免费额度与可用次数以豆包官方当前规则为准。

---

## 四、路线 C：百度秒哒 / DuMate

### 秒哒：直接生成并发布

秒哒是百度的无代码应用搭建平台，网址 https://www.miaoda.cn 。对话生成网页后可直接发布。

1. 打开秒哒，登录百度账号。
2. 在对话框粘贴**第三节里那份提示词**（完全相同，可直接使用）。
3. 它生成需求文档后点「立即创作」，等待生成完成。
4. 在预览里检查手机端效果，用自然语言提出修改，例如「倒计时再大一点」「流程卡片的间距调小」。
5. 点右上角「**发布**」，选择生成公开链接，得到形如 `xxx.appmiaoda.com` 的网址。

> 免费额度和发布次数以秒哒官网当前规则为准，早期资料提到每日公开链接有次数限制。

### DuMate：适合本地处理，发布要多一步

DuMate（中文名「搭子」，官网 https://cloud.baidu.com/product/dumate.html ）是桌面级 AI 智能体，擅长操作本地文件、批量整理资料。它可以生成一套完整的静态网站文件，但**交付物默认落在你自己的电脑上**，通常需要再走一次发布才能得到网址。

推荐用法：

```text
我电脑上有一个做好的婚礼请柬文件夹，路径是：［把文件夹拖进来或填写路径］

请帮我：
1. 把 index.html 和 app.js 里的新人姓名改成［姓名］，婚礼日期改成［日期］，地点改成［地点］，地图链接改成［链接］；
2. 检查 375px、390px、430px 三种手机宽度下的显示效果，有溢出或遮挡就修正；
3. 完成后给我一套可以发布的静态文件，并把它们发布成一个能在手机上打开的 HTTPS 网址，把链接告诉我。
```

如果它只能生成本地文件、无法直接发布，就把生成出来的文件夹交给 WorkBuddy（路线 A）发布，或者按 [定制教程第十一节](./GUIDE.zh-CN.md#十一发布到-github-pages) 上传到 GitHub Pages。

---

## 五、没有这些工具？通用兜底方案

任何能写代码的 AI（包括上面没提到的）都适用：

1. 让 AI 生成一个**单文件 HTML**（所有样式和脚本都写在这一个文件里），同样用第三节的提示词。
2. 把文件保存到本地，双击确认能正常打开。
3. 二选一拿到网址：
   - 把这个文件所在的文件夹交给 WorkBuddy，说「发布上线，生成分享链接」；
   - 或者上传到 GitHub Pages（[教程第十一节](./GUIDE.zh-CN.md#十一发布到-github-pages)）。

单文件 HTML 的好处是只有一个文件，不容易漏资源，微信里传也不会丢样式。

---

## 六、拿到网址后必做的 5 件事

1. **自己先点一遍**。用手机打开链接，走完首页、音乐、声呐探索、地图、登记五处。
2. **找 2～3 位亲友试开**。至少覆盖一台苹果、一台安卓，确认不是只有你的手机正常。
3. **只在微信里发网址**。不要发 HTML 文件、压缩包或 GitHub 仓库地址，那些在手机上打不开成网页。
4. **做一张带二维码的图**。朋友圈和纸质请柬上放二维码，比让长辈手动输网址可靠得多。
5. **改完内容记得重新发布**。亲友如果还看到旧版本，让他刷新页面或关掉微信重新打开链接。

微信分享卡片、二维码制作、可直接复制的邀请文案，见 [SHARE.zh-CN.md](./SHARE.zh-CN.md)。

---

## 七、排错速查

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| AI 只改了文件，没给链接 | 没说「发布 / 上线 / 生成链接」 | 补一句「请把它发布成一个可以分享的网址」 |
| 链接打不开 | 刚发布，服务还没就绪 | 等十几秒到一分钟再刷新 |
| 手机上排版错乱 | 生成时没强调手机适配 | 让它「按 375px、390px、430px 重新检查手机显示」 |
| 图片不显示 | 素材是 AI 编造的、或路径不对 | 换成自己上传的图片，或用 CSS / Emoji 绘制 |
| 亲友说看到旧内容 | 浏览器或微信缓存 | 让他刷新页面、关闭微信重开，或换浏览器打开 |
| 打开后有音乐自动播放失败 | 浏览器限制自动播放 | 正常现象，页面里有播放按钮，点一下即可 |
| 登记信息自己看不到 | 本来就只存在访客手机里 | 需要收集名单时，要另外接表单或后端，并告知宾客 |
| 页面用了游戏素材被提示风险 | 版权问题 | 按 [COPYRIGHT_NOTICE.md](./COPYRIGHT_NOTICE.md) 替换为原创素材 |

---

## 八、最后一句提醒

不管用哪个工具，生成的网址都是**公开可访问**的：任何拿到链接的人都能打开。发送前请确认页面上没有真实住址门牌号、手机号、精确地图坐标或婚礼日期等你不愿公开的信息。

让 AI 生成时，那句「不要使用任何游戏角色、动漫形象或受版权保护的素材」建议保留 —— 它能避免绝大多数版权麻烦。
