# 完全零基础的新手定制教程

这份教程写给从未使用过 GitHub、不会写代码、也不了解 HTML 的使用者。你不需要先学编程，只要会下载文件、搜索文字、替换图片即可。

## 一、开始前需要准备什么

建议提前整理到一个文件夹：

- 新人中文姓名和英文名
- 婚礼日期、星期、仪式时间
- 酒店或宴会厅完整名称
- 高德地图或其他地图分享链接
- 当天流程，例如迎宾、仪式、午宴
- 一张适合分享的横版或方形封面图
- 有权使用的背景音乐
- 如需真实宾客登记，准备一个合规的数据存储方案

## 二、下载项目

1. 打开 GitHub 仓库。
2. 点击绿色 **Code** 按钮。
3. 点击 **Download ZIP**。
4. 解压下载的压缩包。
5. 建议先复制一份作为备份，再开始修改。

## 三、用什么软件修改

推荐免费软件 **Visual Studio Code**，也可以使用系统记事本。

使用 VS Code 时：

1. 打开 VS Code。
2. 选择“文件 → 打开文件夹”。
3. 选择解压后的项目文件夹。
4. 左侧会出现 `index.html`、`style.css`、`app.js` 和 `assets`。

## 四、替换姓名、日期和地址

打开 `index.html`，使用搜索功能逐项替换：

| 搜索演示文字 | 替换成你的内容 |
| --- | --- |
| `新郎 & 新娘` | 新人中文姓名 |
| `新郎&新娘` | 结尾显示的新人姓名 |
| `2030年10月1日 · 星期二` | 婚礼日期和星期 |
| `2030.10.01` | 首页及结尾短日期 |
| `示例市蓝湾婚礼中心·深海厅` | 婚礼地点 |
| `示例市 蓝湾婚礼中心 深海厅` | 地图卡片地点 |
| `https://ditu.amap.com/` | 你的地图分享链接 |

不要删除 `<`、`>`、引号或标签，只替换标签中间的文字。

## 五、修改倒计时和住宿时间

打开 `app.js`。

第一行是婚礼时间：

```js
const weddingDate = new Date('2030-10-01T11:58:00+08:00')
```

将日期与时间换成自己的婚礼时间。`+08:00` 表示中国标准时间，国内婚礼通常保留即可。

继续搜索：

```text
2030-09-30T14:00
2030-10-01T12:00
```

分别替换为默认入住与退房时间。

## 六、修改当天流程

在 `index.html` 搜索 `10:30` 和 `11:58`，可以修改时间与说明。

例如：

```html
<time>10:30</time>
<div><b>海面集合</b><span>迎宾 · 合影 · 与新人会合</span></div>
```

建议每条说明不要太长，手机上更容易阅读。

## 七、替换新人姓名图片和分享图片

### 首页姓名图片

当前首页使用：

```text
assets/demo-wedding-names.svg
```

最省事的方式是制作一张透明背景 PNG，然后：

1. 把图片放进 `assets` 文件夹。
2. 例如命名为 `wedding-names.png`。
3. 在 `index.html` 搜索 `demo-wedding-names.svg`。
4. 改为 `wedding-names.png`。

推荐使用宽横版透明图片，文字周围保留适当空白。

### 微信和社交分享图片

打开 `index.html`，搜索 `og:image` 和 `twitter:image`，把网址换成部署后能够公开访问的图片地址。

微信可能缓存旧标题和图片。修改后仍显示旧内容时，可以换一个链接参数测试，例如：

```text
https://你的网址/?v=2
```

## 八、替换背景音乐

浏览器通常不允许网页在没有用户操作时直接播放音乐，因此本项目会在用户点击开始任务或音乐按钮后播放。

推荐做法：

1. 确认音乐拥有公开使用授权。
2. 将音乐压缩成体积较小的 MP3。
3. 放入 `assets`，例如 `assets/wedding-bgm.mp3`。
4. 在 `index.html` 找到 `<audio id="official-bgm"`。
5. 将 `src` 改成：

```html
src="./assets/wedding-bgm.mp3"
```

不要直接把流媒体平台下载的歌曲或游戏原声上传到公开仓库，除非已经获得相应授权。

## 九、宾客登记如何变成真的

当前仓库是演示模式：表单只保存在访客自己的浏览器，数据不会上传。

> 💡 **零基础最快方案**：用 WPS 文档、腾讯文档、飞书或问卷星建一张宾客登记表，然后只在 `app.js` 粘贴一行公开填写链接。请勿删除原登记表或整段替换代码。四个平台的具体步骤见 [零基础宾客管理后台教程](./RSVP_BACKEND.zh-CN.md)。

如果需要真实收集数据，也可以选择：

- 自己的服务器和数据库
- Cloudflare Workers + D1
- Supabase、Firebase 等后端服务
- 经过评估的在线表单服务

真实上线前至少要做到：

1. 使用 HTTPS。
2. 不在前端代码中写数据库密码、密钥或管理员密码。
3. 只收集婚礼安排真正需要的信息。
4. 告知宾客收集目的和使用范围。
5. 限制后台访问人员。
6. 婚礼结束后及时导出并删除不再需要的数据。
7. 防止重复提交、垃圾请求和批量读取。

只修改页面文字并不能建立真实数据库。如果你不懂后端，建议先保留演示模式，避免误以为信息已经成功提交。

## 十、在电脑上预览

最简单的方法是双击 `index.html`。

如果音乐或某些资源无法加载，可使用 VS Code 的 Live Server 扩展启动本地预览。请不要把管理员密码、API 密钥或真实宾客名单放入项目文件夹。

## 十一、发布到 GitHub Pages

### 第一次上传

1. 登录 GitHub。
2. 点击右上角 `+`，选择 **New repository**。
3. 输入仓库名。
4. 公开展示请选择 **Public**。
5. 创建仓库。
6. 点击 **uploading an existing file**。
7. 上传项目中的全部文件与文件夹。
8. 填写提交说明并确认上传。

### 开启网页

1. 进入仓库 **Settings**。
2. 左侧选择 **Pages**。
3. Source 选择 **Deploy from a branch**。
4. Branch 选择 `main`。
5. Folder 选择 `/ (root)`。
6. 点击 **Save**。

发布通常需要几分钟。最终地址一般是：

```text
https://你的用户名.github.io/仓库名/
```

## 十二、绑定自己的域名

在 GitHub Pages 设置中填写域名，再到域名服务商添加 GitHub 要求的 DNS 记录。DNS 变更可能需要一段时间生效。

不要同时让同一个子域名指向多个不同平台。修改前建议保存现有 DNS 记录截图，避免影响正在使用的网站。

## 十三、做好以后如何分享给亲朋好友

宾客需要收到的是**部署后的 HTTPS 网页地址**，不是电脑上的 `index.html`，也不是 GitHub 仓库地址。

最稳妥的分享流程是：

1. 完成部署，取得类似 `https://你的项目.pages.dev/`、`https://你的用户名.github.io/仓库名/` 或自有域名的地址。
2. 先请 2～3 位亲友用不同手机和不同网络试开。
3. 检查音乐、地图、底部导航和宾客登记。
4. 再通过微信私聊、群聊或二维码海报正式发送。
5. 正式发送后不要随意更换网址，否则已经发出的链接和二维码可能失效。

微信是否显示标题、简介和封面卡片，最终由微信客户端和域名状态决定。普通静态网页不能保证每一次转发都出现自定义卡片，因此建议同时准备“邀请文字 + 网页链接 + 二维码海报”三种形式。

详细步骤、可直接复制的微信文案、二维码制作和排查方法见：

> 💌 [做好邀请函后，如何分享给亲朋好友](./SHARE.zh-CN.md)

## 十四、常见问题

### 页面只有文字，没有样式或图片

检查 `index.html` 中的路径是否保留了 `./`，并确认文件名大小写完全一致。GitHub Pages 区分大小写。

### 手机地点换行

适当缩短地点，或在 `style.css` 中搜索 `.hero-venue` 调小字号。不同系统字体宽度不同，过长文字无法保证在所有手机上保持完全相同。

### 音乐没有自动播放

这是手机浏览器的正常限制。让用户点击“开始任务”或右下角音乐按钮即可。

### 微信分享图没有更新

确认图片使用完整 HTTPS 地址，图片能够公开访问，并使用带新参数的链接重新测试。微信缓存更新时间无法由网页完全控制。

### 宾客说已经登记，但后台没有数据

公开 Demo 默认没有后台，只保存到访客自己的浏览器。必须单独接入后端，才能集中查看宾客登记。

## 十五、发布前检查清单

- [ ] 姓名、日期、星期正确
- [ ] 地址和地图链接正确
- [ ] 流程时间正确
- [ ] 手机首页没有严重遮挡
- [ ] 图片和音乐拥有使用权限
- [ ] 分享标题、简介和图片已替换
- [ ] 分享的是公开 HTTPS 网页地址，不是本地文件或 GitHub 仓库地址
- [ ] 已用苹果、安卓手机及移动网络各测试一次
- [ ] 二维码扫描后能打开最终网址
- [ ] 正式登记接口已测试
- [ ] 仓库中没有密码、密钥和真实宾客名单
- [ ] 已阅读版权风险说明

完成这些检查后，再把链接发送给宾客。
